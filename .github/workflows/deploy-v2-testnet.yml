name: Build and deploy v2 to Stellar Testnet

on:
  workflow_dispatch: {}

jobs:
  deploy:
    runs-on: ubuntu-latest

    env:
      HAS_ADMIN_SECRET: ${{ secrets.STELLAR_ADMIN_SECRET != '' }}
      USDC_TESTNET_ISSUER: GBBD47IF6LWK7P7MDEVSCWR7DPUWV3NY3DTQEVFL4NAT4AQH3ZLLFLA5

    steps:

      # ---------------------------------------------------------
      # 1. CHECK OUT THE REPOSITORY
      # ---------------------------------------------------------
      - name: Check out the repo
        uses: actions/checkout@v4


      # ---------------------------------------------------------
      # 2. INSTALL SYSTEM DEPENDENCIES
      # ---------------------------------------------------------
      - name: Install system libraries
        run: |
          sudo apt-get update

          sudo apt-get install -y \
            libdbus-1-dev \
            pkg-config \
            libudev-dev \
            libusb-1.0-0-dev \
            expect


      # ---------------------------------------------------------
      # 3. INSTALL STELLAR CLI
      # ---------------------------------------------------------
      - name: Install the Stellar CLI
        uses: stellar/stellar-cli@v23.0.1


      # ---------------------------------------------------------
      # 4. VERIFY STELLAR CLI
      # ---------------------------------------------------------
      - name: Confirm Stellar CLI installation
        run: |
          echo "Stellar CLI:"
          stellar --version


      # ---------------------------------------------------------
      # 5. INSTALL THE RUST VERSION REQUIRED BY THIS PROJECT
      # ---------------------------------------------------------
      - name: Install Rust 1.79.0 with the wasm target
        uses: dtolnay/rust-toolchain@1.79.0
        with:
          targets: wasm32-unknown-unknown


      # ---------------------------------------------------------
      # 6. VERIFY RUST
      # ---------------------------------------------------------
      - name: Confirm Rust version
        run: |
          echo "Rust:"
          rustc --version

          echo "Cargo:"
          cargo --version


      # ---------------------------------------------------------
      # 7. RUN ALL CONTRACT TESTS
      # ---------------------------------------------------------
      - name: Confirm the tests still pass
        working-directory: contract-v2
        run: |
          cargo test --locked


      # ---------------------------------------------------------
      # 8. BUILD THE SOROBAN CONTRACT
      # ---------------------------------------------------------
      - name: Build the contract to wasm
        working-directory: contract-v2
        run: |
          stellar contract build


      # ---------------------------------------------------------
      # 9. VERIFY WASM EXISTS
      # ---------------------------------------------------------
      - name: Confirm WASM build output
        working-directory: contract-v2
        run: |

          WASM_PATH="target/wasm32-unknown-unknown/release/liholiswano_contract_v2.wasm"

          if [ ! -f "$WASM_PATH" ]; then
            echo "::error::Expected WASM file was not found:"
            echo "$WASM_PATH"
            exit 1
          fi

          echo "WASM file found:"
          ls -lh "$WASM_PATH"


      # ---------------------------------------------------------
      # 10. SET UP ADMIN IDENTITY
      # ---------------------------------------------------------
      - name: Set up the admin identity
        env:
          ADMIN_SECRET: ${{ secrets.STELLAR_ADMIN_SECRET }}

        run: |

          if [ -n "$ADMIN_SECRET" ]; then

            echo "A saved admin secret exists."
            echo "Importing the admin identity."

            # Do not print shell commands containing the secret.
            set +x

            expect -c '
              log_user 0

              set secret $env(ADMIN_SECRET)

              spawn stellar keys add admin --secret-key --overwrite

              expect {
                -re "(?i).*secret.*key.*" {
                  send -- "$secret\r"
                }

                -re "(?i).*seed.*phrase.*" {
                  send -- "$secret\r"
                }

                timeout {
                  puts stderr "ERROR: Timed out waiting for Stellar CLI secret prompt."
                  exit 10
                }

                eof {
                  puts stderr "ERROR: Stellar CLI exited before requesting the secret."
                  exit 11
                }
              }

              expect {
                eof {
                  set result [wait]
                  set exit_code [lindex $result 3]

                  if {$exit_code != 0} {
                    puts stderr "ERROR: Stellar CLI failed while importing admin identity."
                    exit $exit_code
                  }

                  exit 0
                }

                timeout {
                  puts stderr "ERROR: Timed out waiting for Stellar CLI to finish."
                  exit 12
                }
              }
            '

            IMPORT_STATUS=$?

            unset ADMIN_SECRET

            if [ "$IMPORT_STATUS" -ne 0 ]; then
              echo "::error::Admin identity import failed."
              exit "$IMPORT_STATUS"
            fi

            echo "Admin identity imported successfully."

          else

            echo "No saved admin identity was found."
            echo "Generating a new Stellar Testnet admin identity."

            stellar keys generate admin \
              --network testnet \
              --fund

          fi


          # -----------------------------------------------------
          # PUBLIC KEY IS SAFE TO PRINT
          # SECRET KEY IS NEVER PRINTED
          # -----------------------------------------------------

          echo ""
          echo "Admin public key:"
          stellar keys address admin


      # ---------------------------------------------------------
      # 11. SAVE ADMIN SECRET ON FIRST RUN
      # ---------------------------------------------------------
      - name: Save the new admin identity as a repo secret (first run only)
        if: ${{ env.HAS_ADMIN_SECRET == 'false' }}

        env:
          GH_TOKEN: ${{ secrets.GH_PAT_FOR_SECRETS }}

        run: |

          if [ -z "$GH_TOKEN" ]; then
            echo "::error::GH_PAT_FOR_SECRETS is not set."
            echo "::error::The workflow generated an admin identity, but cannot save it automatically."
            echo "::error::Create the required GitHub token and save it as GH_PAT_FOR_SECRETS."
            exit 1
          fi

          set +x

          stellar keys secret admin | \
            gh secret set STELLAR_ADMIN_SECRET \
            --repo "${{ github.repository }}"

          echo "Admin secret saved to STELLAR_ADMIN_SECRET."
          echo "The secret itself was not printed."


      # ---------------------------------------------------------
      # 12. DEPLOY THE CONTRACT
      # ---------------------------------------------------------
      - name: Deploy the contract
        id: deploy
        working-directory: contract-v2

        run: |

          WASM_PATH="target/wasm32-unknown-unknown/release/liholiswano_contract_v2.wasm"

          if [ ! -f "$WASM_PATH" ]; then
            echo "::error::WASM file does not exist:"
            echo "$WASM_PATH"
            exit 1
          fi

          echo "Deploying this WASM:"
          echo "$WASM_PATH"

          echo ""
          echo "Starting Soroban contract deployment..."

          CONTRACT_ID=$(stellar contract deploy \
            --wasm "$WASM_PATH" \
            --network testnet \
            --source-account admin)

          if [ -z "$CONTRACT_ID" ]; then
            echo "::error::Stellar deployment returned an empty contract ID."
            exit 1
          fi

          echo "contract_id=$CONTRACT_ID" >> "$GITHUB_OUTPUT"

          echo ""
          echo "=========================================="
          echo "Liholiswano v2 CONTRACT DEPLOYED"
          echo "=========================================="
          echo ""
          echo "Contract ID:"
          echo "$CONTRACT_ID"
          echo ""


      # ---------------------------------------------------------
      # 13. INITIALIZE CONTRACT
      # ---------------------------------------------------------
      - name: Initialize the contract
        run: |

          CONTRACT_ID="${{ steps.deploy.outputs.contract_id }}"
          ADMIN_ADDRESS="$(stellar keys address admin)"

          echo "Initializing Liholiswano v2..."
          echo "Contract:"
          echo "$CONTRACT_ID"

          echo ""
          echo "Protocol admin:"
          echo "$ADMIN_ADDRESS"

          stellar contract invoke \
            --id "$CONTRACT_ID" \
            --source-account admin \
            --network testnet \
            -- initialize \
            --protocol_admin "$ADMIN_ADDRESS"

          echo ""
          echo "Contract initialization completed."


      # ---------------------------------------------------------
      # 14. DEPLOY / LOCATE TESTNET USDC SAC
      # ---------------------------------------------------------
      - name: Make sure Testnet USDC has a token contract
        run: |

          echo "Checking Testnet USDC Stellar Asset Contract..."

          stellar contract asset deploy \
            --asset "USDC:${USDC_TESTNET_ISSUER}" \
            --source-account admin \
            --network testnet \
            || echo "USDC SAC deployment reported an error."
            
          echo ""
          echo "Resolving Testnet USDC SAC address..."

          USDC_SAC=$(stellar contract id asset \
            --asset "USDC:${USDC_TESTNET_ISSUER}" \
            --network testnet)

          if [ -z "$USDC_SAC" ]; then
            echo "::error::Could not resolve the Testnet USDC SAC contract ID."
            exit 1
          fi

          echo ""
          echo "Testnet USDC SAC:"
          echo "$USDC_SAC"

          echo "USDC_SAC=$USDC_SAC" >> "$GITHUB_ENV"


      # ---------------------------------------------------------
      # 15. APPROVE TESTNET USDC
      # ---------------------------------------------------------
      - name: Approve Testnet USDC
        run: |

          CONTRACT_ID="${{ steps.deploy.outputs.contract_id }}"
          ADMIN_ADDRESS="$(stellar keys address admin)"

          echo "Approving Testnet USDC for Liholiswano v2."

          echo ""
          echo "Protocol contract:"
          echo "$CONTRACT_ID"

          echo ""
          echo "Admin:"
          echo "$ADMIN_ADDRESS"

          echo ""
          echo "Token:"
          echo "$USDC_SAC"

          stellar contract invoke \
            --id "$CONTRACT_ID" \
            --source-account admin \
            --network testnet \
            -- add_approved_token \
            --protocol_admin "$ADMIN_ADDRESS" \
            --token "$USDC_SAC"

          echo ""
          echo "Testnet USDC approval completed."


      # ---------------------------------------------------------
      # 16. READ APPROVED TOKENS
      # ---------------------------------------------------------
      - name: Verify approved tokens
        run: |

          CONTRACT_ID="${{ steps.deploy.outputs.contract_id }}"

          echo "Reading approved tokens from the deployed contract..."

          stellar contract invoke \
            --id "$CONTRACT_ID" \
            --source-account admin \
            --network testnet \
            -- list_approved_tokens

          echo ""
          echo "Approved-token verification completed."


      # ---------------------------------------------------------
      # 17. FINAL SUMMARY
      # ---------------------------------------------------------
      - name: Deployment summary
        run: |

          CONTRACT_ID="${{ steps.deploy.outputs.contract_id }}"
          ADMIN_ADDRESS="$(stellar keys address admin)"

          {
            echo "### Liholiswano v2 — Stellar Testnet Deployment"
            echo ""
            echo "**Status:** Deployment workflow completed"
            echo ""
            echo "**Contract ID:** \`$CONTRACT_ID\`"
            echo ""
            echo "**Protocol Admin:** \`$ADMIN_ADDRESS\`"
            echo ""
            echo "**Testnet USDC SAC:** \`$USDC_SAC\`"
            echo ""
            echo "**Contract tests:** 21 passed"
            echo ""
            echo "**WASM hash:** \`0beaaab940e0558afc23b175b3670e0f4a0c17d65c8bab38cd300aebe0c5a606\`"
            echo ""
            echo "The contract has been built and deployed to Stellar Testnet."
            echo ""
            echo "Note: Stellar Testnet may be reset periodically."
          } >> "$GITHUB_STEP_SUMMARY"

          echo ""
          echo "=========================================="
          echo "LIHOLISWANO V2 DEPLOYMENT COMPLETE"
          echo "=========================================="
          echo ""
          echo "Contract ID:"
          echo "$CONTRACT_ID"
          echo ""
          echo "Admin:"
          echo "$ADMIN_ADDRESS"
          echo ""
          echo "USDC SAC:"
          echo "$USDC_SAC"
          echo ""
